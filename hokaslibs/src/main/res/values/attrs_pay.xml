<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="PayRadioButton">
        <attr name="pr_title" format="reference|string" />
        <attr name="pr_logo" format="reference|color" />
        <attr name="pr_desc" format="reference|string" />
        <attr name="pr_checked" format="boolean" />
        <attr name="pr_radio" format="reference"/>
    </declare-styleable>

    <!-- Standard orientation constant. -->
    <attr name="orientation">
        <!-- Defines an horizontal widget. -->
        <enum name="horizontal" value="0" />
        <!-- Defines a vertical widget. -->
        <enum name="vertical" value="1" />
    </attr>

    <declare-styleable name="PayRadioGroup">
        <!-- The id of the child radio button that should be checked by default
             within this radio group. -->
        <attr name="checkedPayButton" format="integer" />
        <!-- Should the radio group be a column or a row?  Use "horizontal"
             for a row, "vertical" for a column.  The default is
             vertical. -->
        <attr name="orientation" />
    </declare-styleable>
</resources>